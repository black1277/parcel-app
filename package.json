{
  "name": "parcel-app",
  "private": "true",
  "source": "src/index.html",
  "targets": {
    "default": {
      "distDir": "./public",
      "publicUrl": "/"
    }
  },
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "cy:run": "cypress run",
    "cy:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^15.13.0",
    "parcel": "^2.5.0"
  }
}
